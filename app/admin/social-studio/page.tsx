import SocialStudioClient from './SocialStudioClient';

export const metadata = { title: 'Social Studio — Stephano.io' };

export default function SocialStudioPage() {
    return <SocialStudioClient />;
}
